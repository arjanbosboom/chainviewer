# JSON-RPC Client

The JSON-RPC client is transport-only. It knows how to build and send requests, but it does not know anything about a specific chain.

Chain-specific RPC details belong in an adapter.